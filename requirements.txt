django>=5.0
# xhtml2pdf removed because it requires system-level 'cairo' which is not available on Vercel.
# PDF generation will be disabled on the web version.

gunicorn>=22.0
whitenoise>=6.5
google-auth>=2.0
requests>=2.31.0
python-dotenv>=1.0
dj-database-url>=2.1.0
psycopg2-binary>=2.9.9
